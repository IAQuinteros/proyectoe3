<div class="container mt-4">
  <div class="card text-bg-light mb-3 mx-auto" style="max-width: 24rem;">
    <div class="card-header">Crear comentario</div>
    <div class="card-body">
      
      <% if @comment.errors.any? %>
        <div class="alert alert-danger">
          <h5>Errores al crear nuevo comentario:</h5>
          <ul>
            <% @comment.errors.full_messages.each do |cmt| %>
              <li><%= cmt %></li>
            <% end %>
          </ul>
        </div>
      <% end %>

      <%= form_with(model: @comment, local: true) do |form| %>
        <div class="mb-3">
        <div>
          <%= form.hidden_field :date_create, value: Date.today %>
        </div>
        <div>
          <%= form.label :user_id, "Username de usuario: " %>
          <%= form.select :user_id, User.all.map { |u| [u.username, u.id]}%>
        </div>

        <div>
          <%= form.label :publication_id, "Id de publicacion: " %>
          <%= form.select :publication_id, Publication.all.map { |p| ["#{p.user.username}-(id: #{p.id})", p.id]}%>
        </div>

        <div>
          <strong><%= form.label :content, "Mensaje" %></strong><br>
          <%= form.text_area :content, class: "form-control" %>
        </div>

        <div class="d-flex justify-content-between">
        <%= link_to 'Volver', comments_path, class: "btn btn-secondary btn-sm" %>
          <%= form.submit "Guardar", class: "btn btn-primary btn-sm", id: "liveAlertBtn" %>
        </div>
      <% end %>